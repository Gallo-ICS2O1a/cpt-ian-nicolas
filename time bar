x=150
speed = 1
def setup():
    size(400, 400)
def draw():
    global x, speed
    background(255)
    fill(0)
    rect(100, 100, 150, 40)
    fill(255)
    rect(100, 100, x, 40)
    x -= speed
    if x == 0:
        speed = 0
